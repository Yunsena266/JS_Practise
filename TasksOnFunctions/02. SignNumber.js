/*
 Write a code which accept a number and return it sign.
 */
let answ=prompt("Enter the number, pls","0");
let answw= (answ>0) ? 'Positive':  (answ<0) ? 'Negative':(answ==0) ? '0': 'Zero';
alert(answw);
